To use the test server, simply start this server with `cargo run` and 
then use this address `http://localhost:4321/api/1/` when initializing 
tesla-rs from your app.

## TODO
- Configurable port
- Configurable car name
- etc. ...
